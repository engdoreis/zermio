{%- set type_ = inner.type_ %}
{%- if !inner.registers.is_empty() %}

//! These are the registers present in the {{inner.type_}} 
//! | Register name      | Description  | Offset | Write | Read| 
//! | :---               | :---         | :---   | :--   | :-- |
{%- for reg in inner.registers %}
  {%- set info_len = reg.info.len() %}
  {%- if info_len > 1 %}
//! | {{reg.info[0].name.trim_end_matches('0')}}[0..{{info_len}}] | {{reg.info[0].desc}} | {{reg.info[0].offset|hex}} | {{reg.is_writable()}} |{{reg.is_readable()}} |
  {%- else %}
//! | {{reg.info[0].name}} | {{reg.info[0].desc}} | {{reg.info[0].offset|hex}} | {{reg.is_writable()}} |{{reg.is_readable()}} |
  {%- endif %}
{%- endfor %}

use super::zermio;
  {%- set reg = inner.registers[0].info[0].name.to_lowercase() %}
  {%- set field = inner.registers[0].bitfields[0].name.to_lowercase() %}
/// ```rust,ignore
/// {{type_|lower}}.{{reg}}.update(|{{reg}}| {
///     if {{reg}}.{{field}}().read() == 1 {
///        {{reg}}.{{field}}().write(0);
///     }
/// });
/// ```
{%- endif %}
pub struct {{type_|pascal_case}} {
    addr: u32, 
{%- for reg in inner.registers %}
    /// {{ reg.info[0].desc }}
  {%- set info_len = reg.info.len() %}
  {%- if info_len > 1 %}
    pub {{ reg.info[0].name.trim_end_matches('0')|lower }}: [{{ reg.info[0].type_|lower }}::{{ reg.info[0].type_|pascal_case }}; {{info_len}}],
  {%- else %}
    pub {{ reg.info[0].name|lower }} : {{ reg.info[0].type_|lower }}::{{ reg.info[0].type_|pascal_case }},
  {%- endif %}
{%- endfor %}
{%- for win in inner.windows %}
    pub {{ win.name|lower }} : {{ win.type_name|lower }}::{{ win.type_name|pascal_case }},
{%- endfor %}
}

impl {{type_|pascal_case}} {
    pub fn new(instance: u32) -> Self {
    let addr = instance;
    Self {
      addr,
{%- for reg in inner.registers %}
  {%- set info_len = reg.info.len() %}
  {%- if info_len > 1 %}
      {{ reg.info[0].name.trim_end_matches('0')|lower }} : [
      {%- for info in reg.info %}
      {{ info.type_|lower }}::{{ info.type_|pascal_case }}::new(addr 
      {%- if info.offset > 0 %}
        {{- " + " ~ info.offset|hex}}),
      {%- else %}
        {{- ")," }}
      {%- endif %}
      {%- endfor %}
      ],
  {%- else %}
      {{ reg.info[0].name|lower }} : {{ reg.info[0].type_|lower }}::{{ reg.info[0].type_|pascal_case }}::new(addr 
    {%- if *reg.info[0].offset > 0 %}
      {{- " + " ~ reg.info[0].offset|hex}}),
    {%- else %}
      {{- ")," }}
    {%- endif %}
  {%- endif %}
{%- endfor %}
{%- for win in inner.windows %}
      {{ win.name|lower }} : {{ win.type_name|lower }}::{{ win.type_name|pascal_case }}::new(addr + {{win.offset|hex}} ),
{%- endfor %}
    }
  }

  pub fn destroy(self) -> u32 {
    self.addr
  }
}
